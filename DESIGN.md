# 无限心动 · 设计文档

> 项目名称：无限心动（infinite-date）
> 项目代号：infinite-date
> 参考架构：Heartmorrow
> 创建日期：2026-07-29

---

## 一句话

去掉固定人设和固定剧情，用无限流世界观驱动，玩家通过系统引导式对话创建想攻略的角色，与NPC短信、邮件、约会。NPC可以是全局共享的（公共态）、个人魔改的（override私有态）、或完全私密不可见的（完全私有态）。

---

## 一、设计动机

约会引擎（短信/邮件/约会/记忆压缩/PlayerFacts）的核心架构经过验证是可行的。本项目从头新建，用运行时动态生成的系统替代固定内容。

无限流世界观天然解决了"为什么不同世界的人能在一起"的缝合问题——不需要解释，主城就是万界枢纽。

---

## 二、核心设计

### 2.1 NPC三种形态

NPC有三种形态。区别在于**人设的归属和可见性**——关系和记忆从一开始就是per-player的，不管哪种形态都有。

```
                        公共态              override私有态        完全私有态
                        ─────────          ──────────────        ──────────
人设来源                 characters表        characters表          character_player_data
                        全局共享            + override覆盖         独立存在，不进characters表
可见性                  所有人              只有该玩家             只有该玩家
创建方式                任何玩家消耗权限创建  改公共NPC人设或创建时选override  玩家消耗权限从零创建
消耗权限                是                  是                    是
关系/记忆               per-player          per-player             per-player
```

**公共态：**
- 人设全局共享，所有玩家都能遇到、约会
- 任何玩家都可以创建公共NPC——消耗权限，创建后所有人可见
- 玩家与公共NPC的关系和记忆是per-player独立的
- **公共NPC不属于任何人**——创建后就是公共资源，被举报OOC时管理员可以修改人设
- **创建附赠一次免费override**：创建公共NPC时，创建者可以顺便定制一个属于自己的override版本，不额外消耗权限。之后就和其他玩家一样了

**override私有态：**
- 玩家修改公共NPC的人设时，该NPC对这名玩家进入override私有态
- 消耗权限（创建时的附赠override除外）
- 创建override时**fork一份完整角色卡**到 `character_player_data`——不是只存差异，是完整拷贝。之后爱怎么改怎么改，不再回公共版合并
- 公共NPC后续被管理员修改OOC不影响已fork的override——各有各的
- 别人看到的还是公共态版本

**完全私有态：**
- 玩家消耗权限从零创建的NPC，不进入公共characters表
- 别人完全看不到——防社死（比如很私人的原创角色）
- 人设存在 `character_player_data` 表中，以 `source_character_id = NULL` 标记

**NPC副本替换（记忆清除）：**
- 喜欢角色但不喜欢当前剧情走向？消耗权限创建这个NPC的副本
- 副本继承人设，但没有之前的记忆和关系——相当于"重新开始"
- 原有的Chronicle和relationship记录不删除，只是切换到新副本
- **玩家可以选择激活哪个副本**，但同一时间只能激活一个
- **切换副本免费**——创建花了权限，切换只是更新激活标记，不调LLM不产生成本
- **副本之间记忆完全隔离**——副本A不知道副本B发生过的事，NPC"自己"也不知道有其他副本
- **好友关系在新副本继承**——切换副本后好友状态保持，但NPC对新副本的"你"没有记忆，态度回到初见
- **新副本的初始player_description**：创建副本时新relationship记录的player_description为默认值"刚认识的陌生人"（或角色创建时设置的初始关系）

**关于主推重叠：**
- 你和小姐妹都可以跟公共态的流萤约会，各自有独立的关系和记忆
- 如果你想把流萤调得更傲娇一点，你的流萤进入override私有态，小姐妹的还是公共态
- 两个人攻略的"走向"由各自的关系/记忆决定，不取决于形态

**公共NPC人设页：**
- 显示人设属性、创建者
- 可以点赞、评论（类似角色百科）
- 可以看到人设修改历史（谁改了什么，管理员OOC修正也会显示）
- 玩家可以提交修改建议（"这个角色的说话风格不太对"），管理员审核

### 2.2 角色创建：系统引导式

不是填表单，是AI扮成系统助手与玩家对话生成角色卡。

**流程：**
```
系统：「你想邀请谁进入你的轮回？」
玩家：「流萤」
系统：「搜索中……找到了。
       机甲少女，星核猎手，性格温柔但战斗果断，
       有一个不愿提起的过去。需要我调整什么吗？」
玩家：「就这样吧」
系统：「消耗权限若干。流萤已进入公共池，所有人可见。」
```

创建公共NPC就消耗权限。玩家也可以在创建时直接修改人设：

```
玩家：「我想把她调得再傲娇一点」
系统：「[更新后的角色卡预览]
       创建公共NPC消耗权限，附赠override免费。
       流萤进入公共池（所有人可见）+ 你的override私有态版本。继续？」
玩家：「继续」
系统：「流萤已进入公共池，你的override版本已激活。」
```

**原创角色创建示例（引导式提问）：**
```
系统：「你想邀请谁进入你的轮回？」
玩家：「一个原创角色」
系统：「好。别人第一眼看到她时，是什么印象？」
玩家：「不太好接近，很冷淡」
系统：「那信任她的人会看到什么不同？」
玩家：「其实很温柔，会默默记住别人的小事」
系统：「什么情况下她会彻底卸下防备？」
玩家：「被背叛的时候反而不会崩溃，会很安静地收拾东西，像在准备离开」
系统：「记下了。这种'安静收拾东西'——是她紧张的反应，还是决断？」
玩家：「决断吧。她已经做好了决定」
系统：「明白了。[角色卡预览]
       表面冷淡，内核温柔，极端状态下安静而决绝。
       消耗权限，进入公共池。继续？」
玩家：「就这样吧」
```
引导提问按性格三层逐层深入：第一眼印象→surface，信任后→core，极端情况→extreme，追问具体表现→emotional_signals，追问来由→background.shaping

**设计要点：**
- AI可以联网搜索角色资料（IP角色如流萤、鲁迅、孙悟空）
- 允许用户上传补充资料（设定文档、小说片段、图片描述等）
- 角色模板字段对应 `CharacterSchema`：name/age/appearance/personality(surface/core/extreme)/speechStyle(description+examples)/textingStyle(description+examples)/background(origin/shaping/current)/emotional_signals/likes/dislikes/boundaries/goals/quirks/backstory_milestones
- 用户随时可以：
  - 口语化修改（"说话风格再正式一点"）→ AI更新对应字段
  - 手动编辑（直接改某个字段）
  - 点"就这样吧"直接定稿
- AI提出的问题是有引导性的——按性格三层逐层引导：
  - "别人第一眼看到她是什么印象？"（→surface）
  - "信任她的人会看到什么不同？"（→core）
  - "什么情况下她会彻底卸下防备？"（→extreme）
  - 追问具体表现："这种反应具体是什么样子？"（→emotional_signals）
  - 追问来由："为什么会这样？"（→background.shaping因果链）
- 私有化后仍可继续编辑人设——不是一次定稿，玩家在攻略过程中随时可以调。修改直接改 `character_player_data.character_data`（已fork的完整角色卡），不影响公共态

**角色卡JSON结构（`character_data` 字段）：**
```json
{
  "name": "流萤",
  "age": "...",
  "appearance": "外貌概述",

  "personality": {
    "surface": "日常面具——别人第一眼看到的性格",
    "core": "性格底色——信任后/放松后露出的真实",
    "extreme": "极端状态——极度信任、情感崩溃、激烈冲突时才露出的一层"
  },

  "speechStyle": {
    "description": "说话风格概述",
    "examples": [
      {"context": "日常", "line": "示例台词"},
      {"context": "被冒犯", "line": "示例台词"},
      {"context": "在意但不承认", "line": "示例台词"}
    ]
  },

  "textingStyle": {
    "description": "短信风格概述",
    "examples": ["日常措辞", "情绪波动时措辞"]
  },

  "background": {
    "origin": "出身背景",
    "shaping": "关键经历如何塑造了现在的性格——因果链，不是孤立标签",
    "current": "当前生活状态"
  },

  "emotional_signals": {
    "nervous": "紧张时的具体表现",
    "happy": "开心时的具体表现——角色特有的方式，不是泛泛的'会笑'",
    "angry": "生气时的具体表现",
    "moved": "被触动时的具体表现",
    "defensive": "防御/回避时的具体表现"
  },

  "likes": [...],
  "dislikes": [...],
  "boundaries": "...",
  "goals": "...",
  "quirks": "...",

  "backstory_milestones": [
    {
      "label": "基因实验时期",
      "time_description": "流萤被选为萨姆驾驶者之前",
      "summary": "在基因实验室被告知失熵症无法治愈，身体将缓慢消失。被提议用基因组参与机甲项目，以此延续存在的意义",
      "diff": {
        "personality": {
          "surface": "过去版本的表面面具",
          "core": "更脆弱、更恐惧，还没有学会用微笑掩饰"
        },
        "appearance": "穿着病号服的少女，没有战斗痕迹"
      },
      "dramatic_potential": "high"
    }
  ]
}
```

**角色卡结构说明：**

- `personality` 分三层：
  - `surface`：日常面具。NPC在主城日常、初识玩家时呈现的样子
  - `core`：性格底色。信任建立后、放松状态下露出的真实。不是被激发出来的，是本来就在的
  - `extreme`：极端状态。极度信任、情感崩溃、激烈冲突时才露出的一层。定义NPC在角色任务/吵架/深度约会中的行为边界
  - prompt-builder根据当前关系阶段和情境注入对应层。初识注入surface，好友注入surface+core，深度场景注入三层。LLM自然判断切换
- `speechStyle` / `textingStyle` 含对话示范：
  - `description` 是风格概述，`examples` 是具体台词
  - 有示范LLM才不会漂——只说"话少"不够，得给例句
  - 约会场景用speechStyle，短信场景用textingStyle
- `background` 是因果链：
  - `origin`（出身）→ `shaping`（关键经历如何塑造性格）→ `current`（当前状态）
  - 不是孤立标签（"家庭严厉""性格压抑"），是一条线。LLM推断行为时有据可循
- `emotional_signals` 是行为信号映射：
  - 角色在每种情绪下的具体表现——不是"会紧张"，是"紧张时手指无意识摩挲杯沿"
  - 约会场景：信号转为*动作描写*（"她手指摩挲着杯沿"而不是"她感到紧张"）
  - 短信场景：信号转为文字特征（紧张时消息变短、回复变慢、标点变少）
  - 内心独白：信号喂给微反应描写
  - 这套机制替代直接心理陈述——NPC的情绪通过行为传达，不写"她感到X"
- `backstory_milestones` 是角色设定的一部分，存在角色卡JSON里，不单独建表
- 创建时AI根据角色背景判断是否生成里程碑——IP角色有原作过去，通常有多个；原创角色如果有足够背景（人生转折点、未完成的遗憾）也会生成；背景平淡的原创角色可能没有，此时该NPC没有角色任务
- 每个里程碑包含：
  - `label`：简称（显示用）
  - `time_description`：时间描述（什么时期）
  - `summary`：事件摘要（这段过去发生了什么）
  - `diff`：镜像角色卡差异——只存和当前角色卡不同的字段。角色任务触发时，prompt-builder合成镜像NPC = 当前角色卡 + diff覆盖。`personality` 是对象，diff可以只覆盖某一层（如只改`core`，`surface`和`extreme`继承当前角色卡）
  - `dramatic_potential`：戏剧性评估（high/medium/low），影响角色任务是否从该里程碑生成

**初始关系设置：**
- 角色卡定稿后，AI根据角色背景建议初始 `player_description`（写入 `relationships.player_description`，覆盖默认值"刚认识的陌生人"）
- AI建议示例：
  - IP角色原作中有前世关系 → 建议如"前世挚爱"、"曾经并肩作战的同伴"
  - 原创角色有预设关系 → 建议如"青梅竹马"、"失散多年的故人"
  - 没有特殊背景 → 保持默认值"刚认识的陌生人"
- 玩家可以确认或修改——这是创建流程的最后一步：
```
系统：「你和流萤的初始关系是？
       建议：刚认识的陌生人
       你可以接受建议，或自己描述你们的关系。」
玩家：「前世挚爱」
系统：「已设置。流萤对你的初始印象：前世挚爱。」
```

**角色来源分类：**
| 类型 | 处理方式 | 形态 | 权限 |
|------|----------|------|------|
| IP角色（流萤、鲁迅） | AI搜索 → 预填角色卡 → 用户确认/修改 | 公共态 | 消耗 |
| 原创角色（不介意别人看到） | AI引导式提问 → 逐步构建角色卡 → 写入characters表 | 公共态 | 消耗 |
| 原创角色（防社死） | AI引导式提问 → 逐步构建角色卡 → 只存character_player_data | 完全私有态 | 消耗 |
| 混合（以IP为基础但魔改） | IP预填 → 用户指定修改方向 → AI调整 → override | override私有态 | 消耗 |

所有形态的创建都消耗权限。权限 = 你能创建多少个NPC和地点。

**角色创建入口**：通过主神短信（见3.8节）。给主神发短信说"召唤新NPC"→进入系统引导流程。

### 2.3 主城

主城是玩家和NPC的公共活动空间，无限流设定的万界枢纽。

> **这不是多人共享世界的网游，是人设共享的酒馆系统。** 公共NPC的人设全局共享，但每个玩家看到的是自己独立的世界视图——同一个NPC对不同玩家可以显示不同位置、不同行程、不同关系状态。玩家A在主城遇到流萤在咖啡店，玩家B同时遇到流萤在训练场，这不矛盾——他们各自在自己版本的主城里。NPC"人设共享"不等于"世界状态共享"。

**核心设定：NPC知道自己是NPC。**

NPC有自我意识，知道自己是被召唤到主城的。关键在于——**和玩家在一起时，NPC也能快速获得权限**（独自积累极慢）。这给了NPC主动社交的自利动机：主动打招呼、主动邀请约会、主动发短信，都是为了和玩家建立关系从而获得更多权限。不用AI硬编"角色很热情"——动机是自洽的。

**预置内容：** 正式版开局预置一批公共NPC和基础地点（中央广场、咖啡店、观景台、训练场等）。玩家初始权限为0——先熟悉流程和NPC人设，之后通过任务获取权限才能创建自己的NPC和地点。预置NPC和地点都是系统资源，不属于任何玩家。

**NPC行程系统：**
- NPC在主城有自己的行程——去商店、在广场散步、在咖啡店坐着、去训练场
- 行程由NPC性格驱动（喜欢安静的角色更常在僻静处，外向角色更常在热闹处）
- 玩家在主城走动时可能偶遇NPC
- 偶遇时NPC会主动打招呼——不管认不认识。因为和玩家互动本身就对NPC有利
- 已是好友的NPC打招呼更热情，关系一般的也会找话题搭话
- **行程生成方式**：deterministic hash，脚本生成不调LLM
  - 模板池按**性格分类**（不按角色写死——角色是动态创建的）：内向型偏好僻静处，外向型偏好热闹处，战斗型偏好训练场
  - 活动有duration（**分钟级**），不同NPC切换时间天然错开（hash seed不同），不会整点集体换地方
  - 用真实时间驱动（读系统时钟），不需要游戏内时间系统
  - LLM只在特定场景介入：打招呼时生成greeting、世界事件触发行程调整。不是每次进主城都调LLM
  - 不需要预计算全量——只算玩家相关NPC（好友+少量公共NPC），deterministic随用随算
  - **行程落库（v2 起）**：新地图行程生成后落 `scene_schedule_entries` 表（player/char/day_key/location/activity/start/duration/is_llm_edited），与地图、短信、场景约会同一数据源。LLM 调整的行程以 `is_llm_edited=1` 标记不覆盖。旧 `npc_schedules` 表已退役（0 行、无写入点，仅 LLM 调整行程的历史遗留）
- **行程可见性**：
  - 好友：完整行程（名字+位置+活动+场景提示）
  - 非好友（偶遇过/存在于主城）：灰色名字+位置。知道是谁在哪，不知道在干嘛。想聊就过去打招呼
  - 从没见过的NPC：问号。主城里有个人，但你不认识
  - 灰色名字→加好友→变亮+行程解锁，有养成感

**好友系统：**
- 偶遇并交谈后，玩家可以加NPC好友
- **第一次与NPC互动时（偶遇交谈）自动创建relationship记录**，初始player_description为"刚认识的陌生人"。即使不是好友，NPC也会记住上一次互动的态度变化（比如上次聊得不愉快，下一次不会那么热情）
- 加好友后才能短信聊天
- 好友不是永久的——关系太差可以删好友。删好友会彻底抹除与该角色的一切痕迹（好友关系、短信、对话记录、记忆、关系数值、行程、fork），就当没认识过。不可恢复
- **删好友时NPC不会有情绪反应**——因为所有数据已被抹除，NPC不再"存在"于玩家的世界中。与放逐不同，删好友是per-character操作（直接删整段关系），放逐是per-instance操作（只删一个副本）
- 加好友对NPC也有利——好友关系意味着更多互动机会，更多权限

**放逐系统（未实现，见OPEN_QUESTIONS.md #1）：**
- 觉得某个NPC不想再遇到可以放逐——放逐就是删除这个instance
- **放逐是per-instance操作**：一次只删一个副本。就算有10个fork想全删，也要一个一个删
- 放逐时删除该instance的所有per-instance数据（关系、记忆、权限钱包、描述变更、player_facts、向量索引）
- **还有其他active instance** → friendships保留（per-character，其他instance继承好友关系）。和副本切换一致
- **所有instance都删完了** → 自动取消好友关系（friendships设为status='deleted'），NPC从短信联系人列表消失。玩家无法给无active instance的NPC发短信。之后玩家创建新副本时需要重新在主城偶遇并加好友——新副本本来就不认识玩家，重新认识更自然。系统自动取消好友不触发NPC情绪反应（已无instance可执行），与玩家手动删好友不同
- **不可恢复**——删除就是删除，没有撤销
- 其他玩家不受影响

**玩家也可以消耗权限创建地点：**
- 主城内的场所（咖啡店、观景台、训练场...）
- **公开/私有选择**：
  - **公开地点**：消耗较多权限，创建后所有人可见，进入公共行程池。**不归创建者所有**——和公共NPC一样是公共资源，管理员和主神可审查
  - **私有地点**：消耗较少权限，仅自己可见，仅自己的NPC会去
- 地点会进入NPC行程池——NPC会去地点（公开地点所有玩家的NPC都可能去，私有地点只有创建者的NPC去）

**嵌套地点（文件夹式层级）：**
- 地点可以有多层父子结构，像文件夹一样——大地点包含小地点，小地点还可以继续包含更小的地点，深度无限制
- 顶层地点（parent_id 为空）显示在地图首页。进入一个大地点后，看到它里面的子地点列表 + 当前地点的NPC
- **创建子地点**：在任意地点详情页点"在此创建子地点"，新建的地点自动挂到当前地点下。权限消耗规则不变（公开/私有）
- **权限继承规则**：不能在别人的私有地点下创建子地点。公开地点和自己的私有地点下可以自由创建
- **完整路径**：每个地点有从根到当前的完整路径（如"林溯家 › 卧室 › 衣帽间"）。NPC的位置信息、prompt注入的地点列表、约会场景的地点名都用完整路径，确保上下文准确
- **级联删除**：删除一个有子地点的地点时，所有子地点一起删除（ON DELETE CASCADE）
- **典型场景**：玩家创建"XX家"后，在里面建"卧室""书房""阳台"等房间；或者在"星河公园"下建"湖边长椅""花圃区""观星台"等子区域

**NPC也可以消耗权限创建地点：**
- NPC可以创建自己所属世界（或平行世界）中的地点
- 比如流萤可以创建星铁里的"黄金的时刻"，鲁迅可以创建他的书房
- **NPC在自己所属场景里拥有更多权限和行动自由度**——相当于主场优势
- **NPC创建的地点只有这一个fork可见**——消耗的是该NPC实例的权限钱包，创建出的地点绑定到character_instance_id。玩家自己都看不到，除非NPC邀约
- 这些地点进入该实例的行程池，NPC更常去自己的地点
- **玩家不能自主进入NPC的所属世界，也没有相关任务**——只能通过NPC主动邀约才能去，相当于陪NPC"回娘家"
- NPC邀约通过短信发起：「我想带你回我的世界看看，好吗？」
- **NPC所属世界（world_type='npc_home'）的创建时机**：回娘家邀约时创建，或NPC权限攒够时创建

**NPC之间的关系：知道但不是朋友，竞争关系**
- 不建立NPC间关系——不需要 npc_edges 表，不跑NPC间对话
- 不同世界的人认识起来没有话题，而且他们是竞争关系——都在争取玩家的注意力来获取权限
- NPC"知道别人"的实现很轻：NPC的system prompt里注入"主城中有这些NPC：[名字列表]"，LLM自然知道怎么在对话中提及
- 竞争关系是权限动机的自然推论：NPC知道权限来源是玩家注意力，注意力有限，所以会争。但争的方式由性格决定——有的直球邀约，有的暗戳戳吃醋，有的装不在意。不需要额外写进prompt
- 如果玩家提起另一个NPC，当前NPC的反应由性格驱动——可能吃醋、可能不在意、可能好奇。纯LLM自然反应，不需要额外系统
- 未来最轻量的NPC互动方式：NPC在主动短信里提到另一个NPC——「今天在广场看到那个银头发的人了，好像也在等谁」——纯文本提及，不需要跑NPC间对话

### 2.4 任务系统

任务分三种，戏剧性来源不同，不硬统一。

#### 角色任务：填补意难平【未实现】

- 系统发给玩家的**单人任务**
- 任务内容：让玩家进入NPC过去的某个**时间切片**，经历那段意难平（遗憾、失去、未完成的事）
- **玩家遇到的是那个时期的NPC（镜像NPC）**——不是现在的NPC。镜像NPC形态可能不同（如基因实验时期的流萤还没有成为萨姆），也不认识玩家
- **镜像NPC的生成**：从角色卡的 `backstory_milestones` 中选一个里程碑，prompt-builder合成镜像角色卡 = 当前角色卡 + 里程碑的 `diff` 覆盖。镜像NPC用合成后的角色卡注入system prompt，不知道未来的事，不认识玩家
- **里程碑选择**：优先选 `dramatic_potential` 高的。如果没有里程碑（背景平淡的原创角色），该NPC没有角色任务
- 任务世界是NPC过去的场景重建——基于里程碑的 `summary` + `time_description` 生成世界设定（world_type='mission'），类似世界副本的薄壳设定
- IP角色：里程碑来自原作中的过去
- 原创角色：里程碑来自创建时AI根据角色背景生成的人生转折点
- **现在的NPC不参与任务，但会梦到任务的大概**：任务结束后NPC主动发短信。例如流萤："我梦到你救了我，我们在匹诺康尼看了一场盛大的烟花"
- 本质是**填补意难平**——玩家穿越到NPC的过去，经历/改写那段经历。回到现在后NPC只是"梦到"了，记忆没有被真正改写
- **玩家描述挂在镜像上，做梦后转移**：角色任务期间是和镜像fork互动，不变更原NPC的描述。镜像NPC每次回复时LLM输出的 `player_description` 变更只存在于任务session的临时空间中，不写入description_changes表。任务结束后，当前NPC"做梦"——根据镜像期间积累的记忆摘要，由LLM判断产生一次 `player_description` 更新，这唯一的一条记录写入description_changes（source_type='dream'）。当前NPC没有任务中的逐句记忆，但对自己的看法变了，NPC自己用"梦"来解释这种说不清的感觉
- **角色任务的Chronicle是临时的**：任务session生成Chronicle条目（character_id存当前NPC的ID，character_instance_id存NULL），用于做梦时提取记忆摘要。dream生成后这条Chronicle即丢弃（对应的memory_embeddings也删除），不注入当前NPC后续对话prompt。当前NPC只记得梦（player_description变化），不记得任务细节
- 玩家可以选择不去

#### 世界任务：合作冒险

> ⚠️ **实现状态**：世界任务已上线，但实现为**卦象驱动版**（摇卦起卦 → 卦象生成原创世界 → 困境→目标态通关 → 评级）。本节"物品回收/执念"是早期设计，当前机制详见 `docs/HEXAGRAM_MISSION_DESIGN.md`。下表保留早期设计作参考，实际完成判断见下方"卦象驱动版的完成判断"。

- 系统发给玩家，玩家**选一个好友NPC同行**（NPC不会拒绝——和玩家在一起符合NPC获取权限的自利动机）
- **世界设定**：系统生成原创世界（不用IP世界——IP角色太多容易OOC，而且玩家看过了没有探索新鲜感）。每次都是新世界，每个玩家都是第一次去
- **任务目标（卦象驱动版）**：摇卦起卦（纳甲筮法，种子=玩家+时辰+序号）得到一个卦象，把卦象翻译成一个"具体的人（或一小群人）正在承受的生活困境"——有名字、有面孔、有心碎的理由。玩家理解困境、帮助那个人走出困境（困境→目标态）
- **卦象是幕后骨架**：玩家永远看不到卦象。本卦=困境、互卦=暗线、变卦=目标态、错卦=反面镜像（藏进转折伏笔）。六亲（纳甲）映射叙事角色：官鬼=任务核心对象、妻财=所求之人、父母=靠山、子孙=贵人、兄弟=竞争者、世应=立场
- **合作**：玩家和NPC一起经历、一起做决定、一起解决问题——合作自然发生，不是系统强制
- **千辛万苦**：不是系统卡你，是世界的NPC不给你。要谈判、要欺骗、要交换、要感动对方
- 通关 → 评级 → 权限到账
- 玩家描述自然累积（LLM逐句判断是否更新 `player_description`）

#### NPC任务：角色特长，温馨向【未实现】

> ⚠️ **设计已定稿（2026-08-16）**：本节是早期设计草案。定稿版（NPC 主动摇卦生成温馨小任务 → 短信邀请 → 接受/拒绝两分支；触发 = 好友 + 行程空档 + 不打断话题 + 每 NPC 每天一次；拒绝后 NPC 独自完成、roll 完成时长、期间从主城消失/短信收不到）见 `docs/NPC_TASK_DESIGN.md`。

- 系统发给NPC，NPC根据关系主动邀请玩家（玩家可拒绝）
- 任务和**角色本身的能力/性格/过去**相关——知更鸟教别的世界的福利院孩子唱歌、鲁迅去某个世界办识字班、战斗型NPC帮某个世界解决野兽骚扰
- **NPC没有玩家也能完成**——但玩家在会更好。NPC是主角，玩家是帮手
- **玩家拒绝** → NPC独自完成 → 发短信分享喜悦（"今天教孩子们唱歌了，有个小女孩唱得特别好"），不带遗憾
- **玩家接受** → 一起经历 → 更丰富的互动 → 权限到账
- **戏剧性来源**：角色特长 + 温馨场景。展示角色好的一面
- **系统不只是强盗**：也会做好事。世界观更立体——回收物品是能源需求，NPC任务是系统的善意
- 玩家描述自然累积（LLM逐句判断是否更新 `player_description`）

#### 任务的共同原则

- **任务触发方式（世界任务·已实现）**：玩家在主城主动**摇卦起卦**（`POST /missions/divine`）触发，非随机非固定日程。起卦种子=玩家+时辰+序号，同一时辰内可摇多次（序号递增）得到不同卦
- **角色任务/NPC任务（未实现）**：随机触发机制到实现时再定
- 任务不是必须的——在主城直接约NPC也行
- 玩家可以选择不去
- 不强制指定同伴——角色任务NPC不参与，世界任务玩家自选，NPC任务NPC邀请玩家
- 约会结束后Chronicle折叠记忆
- 角色任务/NPC任务不判断完成——经历就行
- **世界任务判断完成+分级评级**（见下）

#### 世界任务的完成判断与评级

世界任务有客观完成条件：**困境浓度降至目标态**（`goal_achieved`）。不存在争议空间。

> ⚠️ **卦象驱动版**（当前实现）：目标态由 `judgeStatsAndAmbient` 逐轮用数值函数判定（`stats_config` / `stats_state`），困境浓度从初始值降到目标态即 `goal_achieved=true`。下方"物品到手/执念了却"表格是早期设计，当前评级为：目标达成（数值判定）+ 合作质量（LLM判断 'poor'/'decent'/'excellent'）。

完成后分三级评估，由约会评估器（LLM）在约会结束时判断：

| 评估项 | 判断方式 | 说明 |
|--------|----------|------|
| 物品到手 | 客观 | 基础权限。没拿到=任务失败，不给权限 |
| 执念了却 | LLM判断 | 强拿的还是主动交出的？伤害了对方还是帮对方释怀了？加分 |
| 合作质量 | LLM判断 | 玩家和同行NPC有没有真正合作？还是各走各的？加分 |

- 基础权限保障（只要拿到物品），但想拿满需要做好
- 评估的是"事做得怎么样"不是"NPC满意不满意"——不变成伺候人
- "千辛万苦"的感觉来源：确实要把东西拿回来，而且做好有额外奖励
- **评级影响任务发放**：评级高→系统发更多更好的任务；评级低→系统减少或限制任务发放（"理由是评级不高"）。不需要额外防刷系统，评级本身就是限制。逻辑自洽——系统看人下菜碟

**防刷：** 物品回收一次性（同一任务不重复）、NPC不愿短时间连续去、不是好友不能接。

#### 世界副本生成

任务世界（世界任务/角色任务/NPC任务）都是 worlds 表中的一条记录，world_type='mission'。字段映射：

| 设计概念 | DB字段 | 说明 |
|----------|--------|------|
| 世界名 | `name` | 世界名称 |
| 环境描述 | `summary` | 世界环境概述 |
| 氛围关键词 | `tone` | 氛围/基调 |
| 特殊规则 | `rules` | 世界特殊规则文本 |
| 背景设定 | `lore` | 世界观/历史/文化等LLM应视为世界数据的文本 |

- 世界任务：系统生成原创世界，每次新世界不重复
- 角色任务：世界来源是NPC的记忆/过去——过去场景重建，NPC过去版本的角色卡作为世界设定的一部分
- NPC任务：系统生成原创世界

**世界副本的生命周期：薄壳设定，无状态机**
- 世界副本只存设定文档（name/summary/tone/rules/lore）。就是个舞台布景
- 不需要world_states表（day/phase/stamina/actions_today...）——世界不是运转中的模拟器，是静态背景
- 每次进入副本 = 新建conversation session + 从Chronicle加载相关记忆
- 约会结束 → session关闭 → Chronicle折叠记忆 → 世界设定保留
- 再进同一个世界 → 新session，旧session不加载（记忆已折叠进Chronicle）
- 之前的事只存在于玩家和NPC的记忆里，世界本身不记得
- 任务世界（mission类型）一般一次性——世界任务每次生成新世界，角色任务/NPC任务的"再进同一个世界"只适用于NPC所属世界

### 2.5 权限系统

**权限的本质是限制玩家能创建的内容总量（NPC + 地点）。** 防止单人无限创建内容压垮GPU。

**权限是双向的：** 玩家有权限，NPC也有权限。NPC和玩家在一起完成任务时，双方都获得权限奖励。NPC独自完成任务获得的权限极少。这是NPC主动社交的核心动机——和玩家在一起能快速积累权限。

**权限用途：**
- 创建公共NPC（消耗权限，所有人可见）
- 创建完全私有NPC（消耗权限，仅自己可见）
- 公共态→override私有态（消耗权限）
- 创建地点（消耗权限，进入NPC行程池。玩家创建主城地点消耗玩家权限，NPC创建所属世界地点消耗NPC权限）
- NPC副本替换（消耗权限，清除记忆重新开始）
- 撤回消息（消耗权限，短期时间回溯，见3.6）
- 独白窥探（消耗权限，查看NPC某条回复的内心独白，见 docs/PROMPTS.md）
- 可能的其他用途待定

**各项消耗不等**：创建NPC要跑LLM搜索+多轮对话生成角色卡，独白窥探只是读已生成的数据——GPU成本差几个数量级。具体数值待实测后定，不同操作消耗不同。

**如果消耗不大：**
- 权限可以退化成通用货币（比如"系统币"），任务给货币，创建NPC花货币
- 甚至直接去掉，不限NPC数量——取决于H100单卡能扛多少角色

**NPC初始权限：** 0。新创建/新加好友的NPC权限钱包从0开始——"刚认识不会带你回娘家"，需要和玩家一起完成任务积累权限后才能创建所属世界地点。

**权限获取：**
- 完成任务获得（世界任务结束后自动发放：`grantPlayerPermission(playerId, totalReward, 'mission_reward')`，同行NPC也获得合作收益）
- 数值已落 `apps/server/src/config/permission_costs.json`（`mission_base_reward: 50` 等），改文件重启即可调

**设计原则：**
- 不设魔法数字阈值——这些数值仍是占位值，实测负载后用数据调整 JSON 即可
- 任务结束自动发放已实现，不再需要手动发放

---

## 三、玩法系统

### 3.1 交互方式

| 玩法 | 说明 |
|------|------|
| **短信** | message_threads + text_messages，逐条弹出到需回复为止 |
| **邮件** | emails表，任务通知/系统指令通过邮件下发 |
| **朋友圈** | moments + moment_interactions表，玩家/NPC发帖、点赞、评论互动 |
| **约会** | conversation_sessions + messages，在主城地点或世界副本中进行 |
| **待办事项** | 系统发布任务/目标，完成后获得权限 |
| **主神** | 通过短信联系，系统功能入口：召唤NPC、世界观说明、账号管理 |

### 3.2 短信

- NPC在主城或副本世界中给玩家发短信
- 逐条弹出（800ms间隔 + 三点跳动动画）
- **自由输入回复**，不是选择支
- 加好友后才能短信聊天
- NPC的短信风格由角色卡的 `textingStyle` 驱动
- **多段回复逐条显示**：NPC一次回复可能发多条短信，不是一次性塞入。前端逐条显示——每条前显示三点跳动typing动画（等待时长按消息长度估算800~2000ms），然后消息出现，停顿300ms再开始下一条。历史消息加载时一次性显示（不回放动画）
- **NPC态度由记忆驱动**（详见3.2.1节）——零数值，LLM读记忆+玩家描述判断态度
- **撤回**：消耗权限撤回最近一轮对话（玩家消息+NPC回复），删消息+回滚玩家描述。NPC不知道被撤回过。详见3.6节

**NPC主动发短信的触发机制：意愿累积 + 后台扫描**

核心原则：**消息生成的时机由NPC状态决定，不由玩家行为决定。** 防止玩家反复上下线刷消息。

每个NPC关系维护 `sms_urge` 和 `moment_urge` 两个概率值（0~100），分别独立累积短信和朋友圈的发送意愿：
- NPC每次结束行程（切换地点）→ 两个意愿各加一点概率（base=1%，乘以性格倍率：外向×1.2，内向×0.8，加随机扰动×0.7~1.3）
- 加完立刻摇骰子：`Math.random() < urge/100` → 发，发完清零
- 没中就继续攒，下次行程变更再加再摇
- 玩家发短信给NPC → `sms_urge` 清零（刚互动完不需要再主动找你）
- 玩家回复NPC朋友圈 → `moment_urge` 清零
- 约会结束 → 照发短信+朋友圈（不受意愿阻断），发完两个都清零

**后台驱动**：`moment-scheduler.ts` 每5分钟遍历所有有好友的玩家，调 `checkScheduleChange(playerId)` 检测行程变化并累积意愿。不依赖玩家在线。

**约会中不主动发短信/朋友圈（v2 起）**：无论新旧约会引擎，只要 NPC 正在和玩家进行中（旧 `conversation_sessions` 或新 `scene_sessions` 未结束），角色**不得**触发任何主动短信或主动朋友圈——面对面时主业是和玩家互动，不该还拿着手机发消息/发圈。实施于 `lib/proactive.ts`：SQL 排除进行中约会的 NPC，意愿累积同样排除。

**防刷**：意愿累积由NPC行程变更驱动，与玩家上下线无关。反复上线100次不会改变 `sms_urge`/`moment_urge` 的值。

**情绪降温：短信意愿随未回应衰减**

NPC 连续主动发消息而玩家一直不回时，不发「体谅放手」式的自贬终结语，而是让短信意愿自然降温——越不理越难攒够意愿，主动消息自然稀疏到几乎停发；玩家一旦回复，计数归零、热情重置：
- 短信阶段只有两档：**正常**（首次主动）→ **疑惑**（此前主动消息未被回应，如「在忙吗？」「怎么不理我？」，语气因性格而异）。不再有「体谅放手」档
- 短信意愿增量 × `decay = 1/(1+连续未回应数)`：0 条没回时照常，1 条减半，2 条 1/3……越不理越难攒
- 朋友圈（`moment_urge`）不衰减
- 玩家回复 → `sms_urge` 清零、连续未回应计数归零、decay 回到 1

**成本**：后台5分钟扫一次，纯DB查询比较位置指纹，不调LLM。只有意愿命中时才花一次LLM调用生成内容。

**架构分层**：NPC主动消息用"deterministic决策 + 按需触发 + LLM只做内容生成"的分层——意愿判断是deterministic的（概率摇骰，不用LLM），命中后才调LLM生成短信内容。

**在线闲置主动消息（presence心跳）**：

除了意愿累积驱动的主动消息，还有一套在线实时机制——玩家正在看聊天界面但闲置不动时，NPC有概率主动开口。

- **前端心跳**：用户在短信/约会界面时，前端每15s发一次presence心跳，报告当前view和闲置时长（idleMs）
- **活动追踪**：前端监听mousedown/keydown/touchstart/scroll/focusin事件，任何活动重置闲置计时
- **输入焦点检测**：输入框有焦点时（INPUT/TEXTAREA/contentEditable），心跳报idleMs=0——用户正在打字或想内容时NPC不会打断
- **触发条件**（全部满足才触发）：
  - 短信闲置 > 180秒
  - 冷却期5分钟（同一角色不频繁主动）
  - 50%概率（让时机有随机感）
- **连续追问机制**：NPC最多连续发2条未得到回应的主动消息——第1条随口开口，第2条注意到沉默后追问（prompt变化），第3条不再发（玩家明显不在，NPC安静下来）。玩家一旦回复，计数归零
- **消息标记**：主动消息在text_messages.metadata中标记`{"proactive":true}`，用于计数和区分

**NPC主动发短信的场景**（eligible命中后，内容由LLM根据NPC当前状态生成）：
- **NPC任务被玩家拒绝后**：NPC独自完成任务（权限奖励极少），回来发短信分享——「今天教孩子们唱歌了，有个小女孩唱得特别好」。不带记恨，语气因性格而异——有的纯粹分享快乐，有的暗藏遗憾（既是对玩家不在的遗憾，也是对权限变少的遗憾）
- **闲置久了**：超过一定时间没互动，NPC有概率主动找你——「在忙吗？」「今天主城的晚霞很好看」
- **行程结束时**：NPC完成一段主城行程（比如从咖啡店出来），有概率发条短信——「刚在观景台喝完一杯咖啡，那家新出的肉桂卷不错」
- **角色任务后做梦**：角色任务结束后NPC梦到大概——「我梦到你救了我，我们在匹诺康尼看了一场盛大的烟花」
- **约会/任务结束后**：NPC和玩家刚一起经历过一段时光，有概率到家后发条短信——像真人约会后发个消息。内容由约会Chronicle摘要驱动：整体氛围、印象深刻的瞬间、未尽的话题。语气因性格和约会质量而异——开心的约会自然分享余韵，尴尬的约会可能只发一句简短的「到了」。角色任务不触发此场景（走做梦机制，不重复）
- **新增好友的greeting短信**：约会中加好友→约会结束后，检测到好友关系存在且短信线程为空，NPC主动发第一条短信。greeting的prompt含约会地点+最后5轮对话作为上下文，自然开启短信线程。异步执行不阻塞约会结束响应

NPC有自己的生活——行程不是摆设，他们真的在主城里过日子。主动短信是他们的生活自然溢出到你这里，不是"想到你了"才发。所以才要限制NPC人数——每个NPC都有独立的行程和状态要维护。

### 3.2.1 关系模型：记忆 + 玩家描述

**没有数值系统。** NPC对玩家的态度全靠记忆和一句话描述驱动，不存任何好感度/情绪数值。

**记忆 → 具体事件流水**
- Chronicle折叠后的记忆摘要：一起经历过什么、对方说过什么、关键转折点
- 驱动**情境反应**——"上次她爽约了""他记得我不吃香菜""我们淋过雨"
- LLM读记忆文本，自然知道当前情境下该怎么反应

**玩家描述 → NPC对玩家的一句话定性**
- 存在 `relationships.player_description` 字段
- NPC自己对玩家的判断，不是系统评分。例如：
  - `"刚认识的陌生人"` → `"还挺有趣的交谈对象"` → `"值得信赖的同伴"` → `"让我又气又在意的人"` → `"不想失去的存在"`
- **一直在变**——每次互动后LLM判断是否更新这句话，可以选择保持原样。吵架后可能变成"现在不太想理的人"，和好后自然回摆
- 驱动**基础态度基调**——下次回复时LLM读到这句话，知道用什么语气和距离感
- 前世恋人初始描述就是`"前世挚爱"`——不需要数字80，一句话LLM完全理解
- 玩家可在关系详情页看到这句话（NPC"怎么看你的"），不显示任何数字

**吵架不等人哄**
- NPC不会因为负面情绪而变成一块等着玩家来哄的石头
- NPC有自己的处理方式——可能赌气发一条不痛不痒的短信、装作什么都没发生转移话题、找个借口主动搭话
- 方式因性格而异：流萤可能赌气发"我才没有在想你"，鲁迅可能发"今天读了本书你可能也会有兴趣"装没事
- **总有人先开口，但先开口不等于认错**——这是真人吵架的感觉
- 这是prompt规则，不是数值判断。LLM读记忆知道刚吵过、知道为什么吵、知道NPC性格，自然判断用什么方式破冰

**为什么不要数值**
- 数值是记忆的低维投影——affinity=50区分不了"信任但不默契"和"默契但不信任"
- LLM读"她记得我不吃香菜"比读"affinity=47"准确得多
- 数值系统引入魔法数字（阈值、升级曲线、衰减公式），都靠拍脑袋
- 去掉数值后：前世恋人初始值问题消失了，吵架扣分问题消失了，通胀问题消失了，非线性问题消失了

### 3.3 邮件

- 系统通过邮件下发任务通知
- 副本完成后的结算邮件
- 区分邮件/短信样式

### 3.3.1 朋友圈

**酒馆模式**：每个玩家只看到自己 + 自己好友NPC的朋友圈。NPC帖子是 per-player 的——同一个公共NPC对不同玩家生成不同朋友圈内容，与 schedule/presence 系统一致。

**帖子作者**：
- 玩家发帖：自由输入内容
- NPC发帖：由系统事件触发，LLM根据NPC角色卡+上下文生成内容（不是玩家发帖的回复，是NPC主动发的朋友圈）

**NPC发帖的触发时机**：
- **约会结束后**：60%概率触发。LLM根据约会Chronicle摘要生成帖子内容——整体氛围、印象深刻的瞬间、约会中的趣事。语气因性格和约会质量而异
- **意愿累积触发**：NPC每次行程变更累积 `moment_urge`，摇骰子命中即发（与短信意愿同机制，但独立累积、独立触发，且不受短信的未回应衰减影响）。内容是NPC行程/日常生活的自然流露

**互动**：
- **点赞**：玩家和NPC都能点赞（toggle）。玩家点赞NPC帖子不会触发NPC回复，只是互动标记
- **评论**：玩家和NPC都能评论
  - 玩家发帖后，好友NPC异步生成评论——每个好友50%概率"刷到"并评论，至少1个好友评论。模拟"刷到了才回复"的延迟（30秒~几分钟）
  - 玩家评论NPC帖子后，帖子作者NPC异步回复评论
  - NPC评论走各自的 characterData + player_description + 记忆，用 prompt-builder 构建 prompt。评论有已有评论作为上下文，避免NPC重复说同样的话

**未读通知**：桌面朋友圈图标显示红点badge，与邮件/短信的badge机制一致。未读定义 = 玩家上次查看朋友圈之后 NPC发的新帖 + NPC新评论/点赞。客户端 localStorage 记录上次查看时间，每8秒轮询 `/moments/unread-count` 获取未读数。打开朋友圈时更新查看时间，badge清零。

### 3.4 约会

**约会的三种触发方式：**
1. **主城偶遇→直接约**：在主城碰到好友NPC，当场约去主城地点
2. **短信约**：好友间短信聊着，提出约会，NPC根据心情和关系决定答应与否
3. **任务驱动**：角色任务（单人）/ 世界任务（选好友同行）/ NPC任务（NPC邀请），进世界副本

- 在任务副本世界或主城地点中进行
- 对话格式：（括号包裹动作描写）+ 说话，动作描写用颜色+透明度区分（不用斜体——中文渲染效果差）
- **不流式**：NPC回复等完整JSON生成后parse再显示。等待期间显示三点跳动typing动画。如实在太慢再考虑优化
- **多段回复逐条显示**：NPC一次回复可能包含多条消息（messages数组），不是一次性全部插入。前端逐条显示——每条前显示typing动画（等待时长按消息长度估算800~2000ms），消息出现后停顿300ms再开始下一条。历史消息加载时一次性显示（不回放动画）
- **自由输入**，不是选择支
- 约会结束后Chronicle自动折叠记忆
- 约会评估器生成约会总结（用于Chronicle摘要和NPC后续态度），不涉及任何数值计算
- **在线闲置主动消息**：与短信相同的presence心跳机制（见3.2节）。约会闲置 > 60秒时NPC有概率主动开口——比如注意到玩家发呆，随口说点什么。连续追问上限2条，玩家不回应就安静下来。输入框有焦点时暂停计时

**约会邀请**（详见 [GROUP_CHAT_DESIGN.md](docs/GROUP_CHAT_DESIGN.md)）：
- 玩家在地点页"邀请约会"入口，可邀请1-2个好友NPC来当前地点约会（不限当前地点有谁，可从别处叫来）
- 选1个走单聊路径（`trigger: 'invite'`，NPC知道自己是被邀请来的，greeting语气与搭话不同）
- 选2个走群聊路径：一次LLM调用同时扮演2个角色，角色之间互相react（接话、反驳、补充），不是各自对玩家独白
- 群聊记忆/关系更新per-character——每个角色各自折叠chronicle、各自更新player_description
- 群聊不支持：任务模式、地点移动、图片发送、nudge/undo/retry
- 群聊通过 `is_group=1` 标记分流 + 独立API端点，单聊逻辑完全不受影响

**约会结束方式：**
- **玩家主动结束**：点"结束对话"按钮，立即结束。最常见的方式
- **场景自然结束**：LLM在回复中判断场景是否自然收束（如NPC说"我们回去吧"），输出建议结束标记。前端展示"对话已自然结束"提示，玩家可选择继续或确认结束
- 世界任务的**物品到手≠约会结束**：物品到手后任务标记完成，但约会继续——玩家可能还有收尾对话、情感交流。玩家点"结束对话"才真正结束约会，触发评估+记忆折叠
- 角色任务/NPC任务没有客观完成条件，完全由玩家决定何时结束

**约会中的锁定机制：**
- 约会进行中**锁定整个手机界面**——无法进入其他app（短信、邮件、地图、待办、设置），无法开启新的约会
- 约会期间收到NPC短信/邮件 → 静默存DB，不弹通知，约会结束后批量弹出
- 撤回是短信功能，约会中不可操作
- 一次只能有一个活跃约会session——必须结束当前约会才能开始新的
- 理由：约会是沉浸体验，打断很出戏。锁定界面让玩家专注于当前对话

### 3.5 待办

- 待完成任务/目标列表
- 任务详情（世界/目标/奖励）
- 完成后权限到账通知

### 3.6 撤回机制

**能撤什么**：最近一次交换——玩家说的一句 + NPC的整条回复（含多气泡）。不是任意历史回退，就是最后一轮。

**入口**：玩家自己发的最后一条消息上，长按或小"撤回"按钮。只有最后一条有，历史消息没有。

**流程：**
1. 点撤回 → 弹确认"消耗权限撤回最近对话？"
2. 确认 → 删除玩家消息 + NPC回复 → 回滚玩家描述到那轮之前 → NPC不知道被撤回过
3. 输入框重新出现，好像那轮对话没发生过

**不能撤的：**
- NPC主动发的短信（玩家没做任何操作，没东西可撤）
- 不是最后一轮的历史消息（不做无限回退）

**NPC主动消息后的撤回**：如果NPC在玩家最后一轮交换后主动发了短信，玩家仍然可以撤回那轮交换——撤回操作只看"玩家最后一次发言"位置，NPC主动消息不算"轮"。撤回删除的是玩家消息+NPC回复，NPC主动消息保留不动。**但撤回会回滚该轮之后所有description_changes**（包括NPC主动消息产生的描述变更）——player_description回到那轮交换之前的状态。

**为什么只限最后一轮**：撤回的本质是"这句话我说错了想重来"，不是"我想重写历史"。限制在最后一轮，实现简单（删最后几条记录+回滚玩家描述），交互清晰（只有一个撤回入口）。想重新开始一段关系是NPC副本替换的功能，不是撤回。

### 3.7 前端结构：手机就是手机

不要多页面路由。整个游戏就是一个手机界面，手机桌面放app图标。

```
┌─────────────────────────┐
│ 🔋 权限: 12              │  ← 状态栏（常驻，类似手机电量）
├─────────────────────────┤
│                         │
│     当前app的内容         │
│                         │
│                         │
├─────────────────────────┤
│ 📋 待办  🗺️ 地图  💌 短信  │  ← 桌面网格（无dock）
│ 📧 邮件  ⚙️ 设置          │
└─────────────────────────┘
```

**状态栏**：屏幕顶部常驻显示权限余额，像手机电量一样随时可见。不需要点进任何app查权限——花权限时状态栏数字直接变（撤回、创建NPC、独白窥探等），获得权限时也实时更新。状态栏右上角可选显示「工作｜灵感」模式开关（默认隐藏，在设置页开启）。

**摸鱼模式（工作/灵感开关）**：
小众需求，但很好玩。开关默认隐藏，玩家在设置页开启后状态栏右上角出现「工作｜灵感」双段式按钮。
- **工作模式**：一键伪装成AI助手界面——白底蓝调聊天UI，欢迎页带建议气泡，有打字动画，接vLLM真实对话。状态栏文字从"无限心动"变"AI助手"，隐藏电量/信号/能量球，老板路过看不出破绽。
- **灵感模式**：正常游戏界面。
- 纯前端localStorage存储开关偏好，后端 `/api/fish/chat` 复用已有LLM适配器，system prompt设为通用AI助手。

桌面app图标列表（类似手机桌面，5个）：
- 📋 待办 — 任务/目标
- 🗺️ 地图 — 地点列表，进入地点触发场景
- 💌 短信 — 角色短信聊天（含主神联系人）
- 📧 邮件 — 系统通知/任务邮件
- ⚙️ 设置

**没有独立app的：**
- **主神** — 通过短信联系，不占桌面位置
- **约会** — 不是入口，是结果。由社交互动触发：
  - 短信约NPC → 对方答应 → 去地图地点 → 约会开始
  - 任务邮件 → 转发给想同行的NPC → 一起去任务地点 → 合作约会
  - NPC主动发短信邀请 → 去地图地点 → 约会开始

约会界面、角色创建、NPC人设页、关系详情是**pushed view**——从某个app点进去，全屏覆盖，有返回按钮。不是独立app。

**消息弹出规则：**
- **widget优先级**：短信 > 邮件 > 目标（"一样一样来"原则——一样处理完再弹下一样）
- **教程完成后，无未读消息则widget不显示**——不占屏幕空间
- **确认后不重播**——已读消息不再弹出动画，避免重复打扰

**关系详情（NPC详情页）**：短信对话界面顶部显示NPC名字（有头像点头像，没头像点名字），点击进入详情页。所有NPC相关操作的统一入口——关系信息（玩家描述、好友状态、共享记忆Chronicle）、人设编辑、副本管理都从这里进。类似微信点头像进个人页。只保留玩家和这个NPC之间的关系和回忆，不做NPC间社交网络和八卦。

理由：
1. 用手机玩，手机界面是原生隐喻
2. 不需要React Router，state-based切换够了
3. "创建角色"不是高频操作，不配占tab

### 3.8 主神

主神是系统在玩家手机上的前端，**通过短信联系，不占桌面位置**。不是app，不是角色——是0号NPC，工具人。

**数据表示**：主神是0号特殊NPC，在 `message_threads` 和 `conversation_sessions` 中 `character_id` 存固定值 `'DEITY'`，不进 `characters` 表。后端识别此值跳过NPC prompt-builder，改用 `deity.system` prompt。

**定位：带选项的自动客服。**
- 结构化功能菜单：召唤NPC
- 自由对话：可以调戏，但主神的回复风格是客服式的——温和而非软弱，简洁有力，不堆砌客套话，偶尔冷幽默
- 不会主动找你聊天、不会情绪化、不会成长——它不是角色

**功能列表：**
- 召唤新NPC（→ 进入2.2系统引导式角色创建流程）
- 世界观说明（无限流设定、权限系统、任务系统解释）—— 内容待定，世界观为玩法服务，先把玩法跑通
- 账号管理（邀请码状态）

> 创建地点在地图App操作，问题反馈在反馈中心提交。主神短信只保留召唤NPC快捷按钮，但玩家仍可在对话中自由询问这些功能，主神引导ta去对应入口。

**前端实现**：主神是短信联系人列表中的一个联系人。但与普通NPC短信界面不同——主神的短信对话界面顶部有快捷功能按钮区（召唤NPC），按钮区下方才是对话区。点功能按钮直接触发对应流程（如打开角色创建pushed view），也可以在下方自由输入短信和主神聊天。普通NPC短信界面没有这个按钮区，只有名字+返回+对话。权限余额不在主神这里查——常驻在顶部状态栏，随时可见。

**世界观自洽**：主神是系统在玩家手机上的前端。NPC知道主神的存在（"系统"），但不会和主神对话——主神只服务于玩家。

### 3.9 开局教程：渐进式解锁

**无主线剧情，但需要引导。** 用主神邮件+渐进解锁引导新手。

**开局流程（一次性，新用户首次进入）：**

```
BootScreen 输入名字
→ 全部 App 锁定（仅设置可用）
→ 主神发欢迎邮件（自动触发，延迟~1秒）
   邮件内容：欢迎来到主城 + 简要说明无限流设定
   邮件结尾引导："去主城中央广场看看吧"
→ 解锁 邮件 + 待办（邮件 banner 弹出 → 点开 → 看完）
→ 解锁 地图（邮件读完后自动解锁）
→ 玩家点地图 → 进入教程场景（中央广场，面对面约会界面）
→ 主神在场景中当面说话（约会对话，LLM回复），玩家可以自由对话
→ 玩家点"结束对话" → 主神身影淡去 → 教程完成
→ 解锁 短信
   主神自动发一条短信（"有事随时找我"）——从面对面到短信的转场
→ 所有 App 永久解锁
```

**玩家在教程中体验所有玩法：**
1. 邮件 — 读主神欢迎邮件
2. 地图 — 点地图进入主城
3. 约会 — 和主神面对面对话（流式LLM回复）
4. 短信 — 教程结束后主神发来短信，建立短信联系
5. 待办 — 邮件读完后生成一个"去主城"的待办，完成后消失

**设计要点：**
- **主神欢迎邮件**：一次性，新用户首次进入时触发。邮件内容包含世界观简介+操作引导
- **主神短信联系人**：教程面对面结束后自动添加，主神发一条"有事随时找我"建立短信联系。之后玩家可以随时给主神发短信问问题（客服式回复）
- **待办目标**：邮件读完后生成一个"去主城"的待办，完成后消失。不强制，但给玩家方向感
- **渐进解锁顺序**：邮件+待办 → 地图 → 短信。地图解锁后进入教程约会场景，教程结束才解锁短信
- **不重新锁定**：教程走完后所有app永久解锁。如果玩家跳过某步（比如不读邮件直接点地图），地图已经解锁就能直接进
- **教程状态追踪**：数据库存储 `tutorial_step` 字段记录教程进度（0=未开始, 1=邮件已发, 2=邮件已读, 3=主城已进, 4=完成）。详见 docs/DATA_MODEL.md

---

## 四、技术架构

### 4.0 玩家认证：邀请码 = 身份

**邀请制，码就是身份，不绑设备。**

- 管理员手动建player记录，每人一个邀请码（码与player_id一对一映射）
- 前端首次打开 → 输入邀请码 → 后端验证 → 返回session token存localStorage
- 清缓存了 → 重新输码 → 后端认得 → 发新token → 数据全在（数据在服务端，不在客户端）
- 码不会过期，管理员可以手动吊销
- 换手机也是输同一个码——码绑的是人不是设备
- localStorage只是"记住我"的便利层，不是身份的载体

**不是Phase 6的"多玩家"**：Phase 6讨论的是共享NPC并发、权限竞争。这里是基础的数据隔离。从第一天后端API就得带player_id参数，否则后面改全是返工。

### 4.1 项目结构

```
infinite-date/
├── apps/
│   ├── server/          ← 后端服务
│   │   └── src/
│   │       ├── config/         ← 权限数值表等配置文件
│   │       │   └── permission_costs.json
│   │       ├── db/             ← schema + 迁移
│   │       ├── llm/            ← LLM适配器（vLLM/OpenAI兼容）
│   │       ├── lib/            ← 核心库（memory/character/schedule/embedding/proactive/wiki-search...）
│   │       ├── prompt/         ← prompt组装框架
│   │       │   ├── builder.ts  ← system prompt + messages 组装
│   │       │   └── templates/  ← prompt文本文件（外部读取，不硬编码）
│   │       ├── routes/         ← API路由（conversation/sms/scenario/mission/moments...）
│   │       └── systems/        ← 业务系统
│   │           ├── creator/    ← 系统引导式角色创建
│   │           └── ...
│   └── web/             ← 前端（手机界面）
│       └── src/
│           ├── components/     ← 共享组件 + admin面板
│           ├── hooks/          ← useChatMessages等共享hook
│           ├── pages/          ← 各页面组件
│           └── lib/            ← API封装等工具库
├── packages/
│   └── shared/          ← 共享类型和schema
└── docs/
    ├── DATA_MODEL.md    ← 数据模型、SQL建表语句
    ├── PROMPTS.md       ← Prompt体系设计
    ├── GROUP_CHAT_DESIGN.md ← 群聊设计
    └── OPEN_QUESTIONS.md ← 未解决问题
```

### 4.2 技术栈

**后端（apps/server）：**
- **Fastify** — Web框架，处理HTTP请求
- **node:sqlite** — Node.js 22+内置SQLite，零原生依赖，数据就是一个.sqlite文件。不用better-sqlite3（需C++编译）、不用Prisma/Drizzle（ORM增加复杂度）。直接写SQL + 薄类型封装
- **Zod** — 请求参数校验。确保API收到的数据格式正确
- **tsx** — 开发时直接运行TypeScript，保存即重启
- **Vitest** — 测试框架

**前端（apps/web）：**
- **React 18** — UI框架
- **Vite** — 构建工具，开发时秒级热更新
- **lucide-react** — 图标库（app图标等）
- 不用react-router-dom（设计文档3.7已定：state-based切换，不要多页面路由）
- 不用i18next（不需要多语言）

**共享（packages/shared）：**
- TypeScript类型定义（CharacterSchema等），后端前端共用一份

**运行时要求：**
- Node.js >= 22（node:sqlite需要）
- pnpm >= 11
- TypeScript 5.7+

### 4.3 模块清单

**已实现的模块：**

| 模块 | 位置 | 说明 |
|------|------|------|
| DB Schema | db/schema.ts | 全部表结构 + FK级联（ON DELETE CASCADE/SET NULL） |
| 迁移管理 | db/index.ts | PRAGMA user_version + try/catch ALTER 渐进迁移 |
| Chronicle记忆压缩 | lib/memory.ts | 每N轮自动折叠历史→叙事摘要，含key_memories/player_facts |
| PlayerFacts | lib/memory.ts | NPC记住玩家在约会中说的话，向量化存储 |
| 向量检索 | lib/embedding.ts | cosine相似度检索记忆，统一top-5返回 |
| 短信引擎 | routes/sms.ts | 短信发送/接收/撤回/retry + greeting（约会结束加好友→NPC主动发短信） |
| 邮件引擎 | routes/email.ts | 邮件下发/读取 |
| 约会对话 | routes/conversation.ts | 约会session管理 + 单聊/群聊 + 搜索增强 |
| 剧本系统 | routes/scenario.ts | 剧本创建/对话/数值判定/梦短信 |
| 探索系统 | routes/explore.ts | 地点探索 + 场景生成 |
| 任务系统 | routes/mission.ts | 卦象驱动世界任务（摇卦起卦→生成世界→困境→目标态通关→评级自动发奖）。角色任务/NPC任务未实现 |
| 朋友圈 | routes/moments.ts | NPC发帖/点赞/评论 + 未读badge |
| LLM适配器 | llm/adapter.ts | vLLM/OpenAI兼容接口，guided_json + 超时 + MAX_MODEL_LEN环境变量 |
| Prompt框架 | prompt/builder.ts | 角色数据→LLM prompt组装，generateReply三层防御 |
| 共享对话函数 | lib/conversation-helpers.ts | undo/retry/saveNpcReply/updatePlayerDescription/search增强 |
| NPC行程系统 | lib/schedule.ts | 模板池+hash+真实时间，性格分类驱动 |
| NPC主动消息 | lib/proactive.ts | 意愿累积机制 + 行程变更检测 + 短信意愿衰减（情绪降温） |
| NPC朋友圈 | lib/proactive.ts（触发）+ routes/moments.ts（生成） | 意愿累积（moment_urge）+ 约会后发帖 |
| 角色搜索 | lib/wiki-search.ts | MediaWiki API联网搜索IP角色资料 |
| 权限系统 | lib/permission.ts + lib/permission-config.ts | 余额管理 + 配置文件兜底 |
| 会话互斥锁 | lib/session-mutex.ts | session级操作互斥 |
| 邀请码认证 | routes/auth.ts | 码→player_id，session token |
| 角色创建 | routes/creation.ts + systems/creator/ | 系统引导式AI对话生成角色卡 |
| 管理后台 | routes/admin.ts | NPC审核/邀请码管理/权限调整 |
| 摸鱼模式 | routes/fish.ts | 伪装AI助手界面 |
| 错误处理 | index.ts | 4xx返回具体message，5xx只返回通用提示 |

### 4.4 运行环境

- H100 80GB 单卡
- vLLM 端口 8000（Gemma-4-26B-A4B-it）
- 后端端口 3000（.env PORT 可配置）
- 前端端口 8080
- /output 持久化，/root 重启清空
- pip install 需要 --user

### 4.5 目标用户

- 小范围联机
- 开源后别人用什么模型、怎么部署不管，商用需自建数据库和后端

---

## 五、开发优先级

### Phase 1：骨架搭建
1. 从头初始化项目（pnpm workspace + server/web/shared）
2. 邀请码认证系统（码→player_id，session token，所有API带player_id）
3. DB schema建表（所有表从第一天就带player_id）
4. prompt组装框架 + 无限流meta层prompt
5. 跑通"空壳"——能创建空世界、空角色、空约会
6. 短信自由输入 + 邮件收发 + 约会对话
7. 渐进式解锁 + 主神欢迎邮件教程（见3.9节）
8. 状态栏（权限余额常驻）

### Phase 2：角色创建 + 权限
1. 系统引导式角色创建后端（creator_sessions + AI对话 + web搜索）
2. 前端系统引导界面
3. 权限系统（先手动发放）
4. 三种NPC形态（公共/override私有/完全私有）打通
5. 地点创建

### Phase 3：主城 + 社交
1. 主城地点体系 + NPC行程系统
2. 偶遇 + 打招呼 + 加好友
3. 好友后解锁短信聊天
4. 逐句玩家描述更新

### Phase 4：任务系统
1. 角色任务：系统发单人任务→玩家进入NPC过去的时间切片→遇到过去版本NPC（形态不同、不认识玩家）→现在的NPC梦到→发短信。IP角色用原作过去，原创角色LLM根据人设生成
2. 世界任务：系统生成原创世界+失去价值的物品+执念→玩家选好友同行→合作回收→权限到账
3. NPC任务：系统发给NPC→NPC邀请玩家→角色特长温馨向（教唱歌/办识字班/帮人）→玩家不去NPC独自完成发短信分享，去了权限到账
4. 世界副本生成 → 进入副本 → 约会/冒险 → 完成 → 权限奖励
5. 短信/邮件适配新系统（NPC梦境短信、NPC邀请短信、任务通知邮件）

### Phase 5：体验打磨
1. NPC主动发短信/邮件
2. 任务多样性（不同世界设定、不同类型执念）
3. 约会总结生成器和记忆系统调优
4. 手机UI适配

### Phase 6（待定）：多玩家
- 目前H100单卡
- 多玩家共享NPC已由数据模型支持
- 等负载有概念后再决定权限数值和并发策略

---

## 六、待定事项

| 事项 | 状态 | 备注 |
|------|------|------|
| 项目名 | 暂定 | 无限心动（infinite-date），不行再改 |
| 世界观设定 | 待定 | 世界观为玩法服务，先把玩法跑通。主神的世界观说明功能内容待填 |
| 权限数值 | 待定 | 等H100负载实测后定；负载不大可退化为通用货币或去掉 |

---

## 七、未解决设计问题

详见 docs/OPEN_QUESTIONS.md。当前5个问题均未关闭，需要实测后决定或实现到对应功能时再定，不阻塞开发。
